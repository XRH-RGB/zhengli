# 后续运行验收清单

状态：**未执行**。本次仅编写源码与静态检查；不将以下项目标记为通过。

1. 配置模式启动：`/api/status` 显示关闭执行；保存仓库成功；同步和运行接口返回 423，GitHub 和模型端均没有请求。
2. 访问控制：设置 `APP_API_KEY` 后，无密钥请求返回 401；正确密钥可访问；跨源请求不能读取或修改数据。
3. 索引：用少量 Markdown/Python 文件同步；验证文件数、片段数和固定提交链接。根目录文件、中文路径与分支名包含 `/` 均需覆盖。
4. 私有仓库：有效只读令牌可读取；无权限/过期令牌返回可理解错误，不显示令牌。
5. 同步失败：制造模型断连、GitHub 限流，旧索引仍可查询；成功同步后删除的文档不再出现在新索引。
6. 重复来源：同一仓库同一提交添加两个文件范围，两个知识源可分别同步和删除，不因片段 ID 冲突失败。
7. 问答：中文与英文问题均返回相关来源，片段与行号一致；不存在依据的问题应明确说明，人工检查模型是否遵循。
8. 模型更换：改嵌入模型/地址并重启后，旧索引查询提示重新同步；维度不一致不会静默继续。
9. 工作流：分别验证七个模块；需要文字材料的场景空输入会报错；结果不宣称完成未接入的外部操作。
10. 界面：检查手机/桌面布局、键盘导航、错误提示、加载状态、复制结果和打开引用。断开后端不会显示假结果。
11. 不可信文本：带 HTML 或指令的仓库文档在界面作为纯文本显示；不得作为代码执行。评估模型对文档提示注入的响应。
12. 并发：同步进行时第二个同步/问答返回 409；单进程配置下不产生并发替换索引。

生产化前另行实现队列、多用户权限、监控与持久化任务状态，不属于本次交付。
